## 1. 项目基础与问卷契约

- [x] 1.1 创建静态页面、共享资源、JavaScript 模块、Supabase 迁移和测试目录结构；通过本地静态服务器验证两个 HTML 入口无需生产构建即可加载。
- [x] 1.2 添加包含 34 道题目的声明式问题目录和十步骤章节目录；通过目录完整性测试验证每个已确认题干、选项、类型、编号、条件和 `Other` 规则。
- [x] 1.3 添加仅用于开发的最小 Node 测试配置和浏览器冒烟测试配置；通过已记录命令验证单元测试套件和空浏览器测试套件能够运行。

## 2. Supabase 存储与访问控制

- [x] 2.1 编写包含答卷行、服务器端身份标准化、时间戳、版本号、JSON 答案和标准化身份唯一性的数据库迁移；通过迁移断言验证重复标准化身份会被拒绝。
- [x] 2.2 实现受控载入和原子保存/修改数据库函数，包括数据验证、版本冲突和首次重复提交处理；通过数据库测试覆盖新建、重新载入、修改、冲突、格式错误答案和同时创建相同身份。
- [x] 2.3 启用行级安全策略、限制匿名直接访问数据表、添加管理员允许列表并仅授予必要的函数/数据表权限；验证匿名批量读取失败，同时精确身份载入和允许列表内管理员读取成功。

## 3. 问卷领域逻辑

- [x] 3.1 以纯模块实现共享身份标准化、问题激活、答案清除和验证；通过单元测试覆盖空格/大小写匹配、全部条件分支、必填选择、选填开放文本和必填 `Other` 说明。
- [x] 3.2 实现本地草稿保存、恢复选择、时间戳比较、丢弃和成功保存后清理；通过单元测试覆盖刷新恢复、身份隔离、较旧草稿处理、成功清理和存储失败容错。
- [x] 3.3 实现 Supabase 公开持久化适配器，以及新建、已载入、保存中、已保存、失败和冲突状态；通过模拟集成测试覆盖成功载入/保存、双击抑制、重试和并发更新拒绝。

## 4. 公开问卷界面

- [x] 4.1 构建英文填写者信息与隐私说明入口，并显示 `Questions? Contact Dr. Song at hui.song@rmit.edu.au.` 邮件链接；验证身份不完整时无法继续、现有答卷会显示已载入提示，且联系链接目标为 `mailto:hui.song@rmit.edu.au`。
- [x] 4.2 构建由目录驱动的单选、多选、评分、开放文本、条件题和 `Other` 说明控件；通过浏览器测试操作每种控件，并确认未激活答案会被清除。
- [x] 4.3 构建固定章节导航、进度、行内错误摘要、第一个无效字段聚焦、向后导航和最终检查链接；通过浏览器测试完成全部十个步骤，并在不丢失其他答案的前提下修改早期章节。
- [x] 4.4 构建提交成功、可重试失败、冲突重新载入和移动端响应式状态；通过浏览器测试覆盖每种状态，并通过无障碍检查验证标签、键盘操作、焦点可见性和错误关联。

## 5. 私有统计界面

- [x] 5.1 构建管理员邮箱/密码登录、无注册流程、身份状态处理、退出和会话过期跳转；通过测试覆盖授权登录、通用失败、未授权访问、退出和过期清理。
- [x] 5.2 实现三个总体指标和全部选择题/评分题分布的纯统计函数；通过单元测试覆盖单选题分母、多选题总占比超过 100%、四舍五入、缺失答案和 `No responses yet`。
- [x] 5.3 构建 `Overview` 和 `Choice Questions` 视图，使用响应式条形图在每个条形后显示选项人数和占比；通过可视化冒烟测试覆盖桌面及移动视口下的无答卷、单份答卷和多份答卷数据集。
- [x] 5.4 构建带署名的 `Open Responses` 搜索/筛选和只读 `Respondents` 详情视图；通过测试覆盖空答案省略、未激活条件答案省略、署名格式、按文字/姓名/单位搜索和时间戳显示。
- [x] 5.5 按固定问题目录顺序实现包含多选与 `Other` 列、可由 Excel 正确打开的 UTF-8 CSV 导出；通过单元测试覆盖引号、换行、Unicode、确定性多选输出和电子表格公式中和。

## 6. 集成、运维与发布准备

- [x] 6.1 添加固定版本的 Supabase 和图表依赖，以及安全的运行时配置处理；验证配置缺失时会显示设置说明，并扫描仓库确认不存在 `service-role` 密钥或管理员密码。
- [x] 6.2 编写运维文档，说明创建 Supabase 项目、应用迁移、创建并加入允许列表的唯一管理员、配置公开项目 URL/匿名密钥、导出备份和使用 GitHub Pages 发布；通过新读者检查清单验证无需未记录步骤即可复现设置。
- [x] 6.3 使用合成数据运行完整的单元测试、数据库策略测试、浏览器测试、并发测试、响应式测试和 CSV 测试；记录准确命令，并在请求 OpenSpec 验证前解决或记录每个失败。
- [x] 6.4 将最终问题目录与源 PDF 和补充文本逐项比较，运行 OpenSpec 严格校验，并验证实现包含全部 34 道题目且没有未经批准的产品范围。

## 7. 管理员答卷回收站

- [x] 7.1 先编写失败的 PostgreSQL/WASM 测试，再添加 `002_response_recycle_bin.sql`：增加删除审计字段、让公开载入忽略回收站记录、让同身份重新提交原子地重新激活记录，并实现仅管理员可调用的回收站读取、软删除、恢复和永久删除函数；验证状态转换、版本冲突、非管理员拒绝和永久删除不可恢复。
- [x] 7.2 先编写失败的单元测试，再扩展管理数据适配器和内存状态：分别载入有效答卷与回收站记录，并提供软删除、恢复和永久删除方法；验证每个 Supabase 调用参数、错误传播、重复点击抑制，以及正常统计和 CSV 永远只接收有效答卷。
- [x] 7.3 先编写失败的浏览器测试，再构建 `Delete` 确认对话框、带数量的 `Deleted Responses` 导航、回收站列表、`Restore` 和姓名匹配的 `Delete permanently`；验证成功后指标立即更新，失败时保留界面，Cancel/Escape 不修改数据，modal 焦点可访问且移动端无横向溢出。
- [ ] 7.4 将增量迁移应用到真实 Supabase 项目，使用合成答卷验证软删除后从全部统计/CSV 消失、恢复后重新计入、同身份重新提交、永久删除、匿名与非管理员拒绝；清理合成数据并记录真实 staging 证据。
- [x] 7.5 运行完整单元、数据库、浏览器、严格 OpenSpec 和密钥扫描验证，提交并通过专用 deploy key 推送 `main`，等待 GitHub Pages 成功部署，再对线上问卷、`/admin.html` 和回收站工作流执行冒烟测试。

## 8. 题目精简与分页

- [x] 8.1 按设计保留 23 道题、删除 11 道候选题；显示编号重排为 1 至 23，内部 id 保持稳定，并同步统计、开放回答与 CSV 预期。
- [x] 8.2 实现章节内分页：每页最多 2 道选择类题，每道开放文本题独占一页；手机与桌面共用顺序、分页、前后导航与按页进度，章节导航跳至该章节第一页。
- [x] 8.3 按当前页校验并聚焦首个无效控件，最终检查按新目录生成并跳回对应章节第一页。
- [x] 8.4 回归分页、章节跳转、页内校验、草稿恢复、条件清理、成功提交及手机和桌面布局。
- [ ] 8.5 回归统计、开放回答编号、填写者详情、CSV 与回收站排除，并确认真实 Supabase 历史答案保留。

### 已有真实环境验证记录
已运行真实项目 staging smoke test：并发提交、匿名访问拒绝、管理员删除/恢复/重新激活/永久删除及合成数据清理通过。已登录非管理员的真实环境权限检查被跳过，故 7.4 保持未完成；本地数据库测试已覆盖。003 已应用，远端迁移历史与本地 001/002/003 一致。8.5 的历史答案保留已由本地集成测试覆盖，真实历史数据尚未逐条核对。

### 发布前规范核对
完整性：23 题、15 页、稳定 id、管理端当前目录、003 校验迁移均已实现；上线验收与上述残余验证另记。正确性：数据校验/条件清理由单元与数据库测试覆盖，浏览器回归覆盖分页、草稿、提交及管理端。连贯性：静态客户端、Supabase RPC、移动布局、答题卡顶部进度条符合设计。此次发布修正两项沿用旧目录/旧导航的浏览器用例；不更改产品逻辑。

## 9. 答题进度条位置

- [x] 9.1 将进度条移动到答题卡顶部；检查窄屏目录在前、进度在后，宽屏左侧目录保持正常，并回归分页和提交。

验证：分页与提交浏览器测试 13/13 通过；320、390、768、1280px 下实际元素位置与页面无横向溢出检查通过；`openspec.cmd validate build-online-competition-survey --strict` 通过。本记录仅覆盖 9.1。

发布前验证：`npm.cmd test` 51/51 通过；`npm.cmd run test:browser` 37/37 通过；两个变更的 OpenSpec strict 校验通过；待发布文件密钥扫描通过。产品提交 e6481b1 已推送，四个变更中的线上 JS/CSS 文件与本地逐项比对一致。线上真实冒烟通过：23 题/15 页提交、管理员登录与统计视图、CSV 当前题目列、软删除排除、恢复、永久删除及合成数据清理均通过。
